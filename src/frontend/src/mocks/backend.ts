import type { backendInterface, RiskTrendEntry } from "../backend";
import {
  DisruptionType,
  MitigationStatus,
  RiskLevel,
  SupplierCategory,
  SupplierTier,
} from "../backend";

export const mockBackend: backendInterface = {
  computeDelayRisk: async (_supplierId) => BigInt(45),
  computeFailureRisk: async (_supplierId) => BigInt(30),
  computeQualityRisk: async (_supplierId) => BigInt(55),
  getCompositeRisk: async (_supplierId) => BigInt(43),

  getMitigationActions: async (_supplierId) => [
    {
      id: BigInt(1),
      status: MitigationStatus.InProgress,
      action: "Qualify backup supplier for electronics components",
      createdAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
      dueDate: BigInt(Date.now() + 14 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now() - 2 * 86400000) * BigInt(1000000),
      supplierId: BigInt(1),
    },
    {
      id: BigInt(2),
      status: MitigationStatus.Open,
      action: "Increase safety stock for raw materials by 20%",
      createdAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
      dueDate: BigInt(Date.now() + 7 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now() - 1 * 86400000) * BigInt(1000000),
      supplierId: BigInt(2),
    },
  ],

  getPortfolioRiskSnapshot: async () => ({
    totalSuppliers: BigInt(24),
    avgCompositeRisk: BigInt(52),
    maxRiskSupplierId: BigInt(3),
    portfolioRiskScore: BigInt(67),
    lowRiskCount: BigInt(8),
    maxRiskScore: BigInt(89),
    mediumRiskCount: BigInt(10),
    highRiskCount: BigInt(6),
    timestamp: BigInt(Date.now()) * BigInt(1000000),
  }),

  getRecommendedAlternatives: async (_supplierId, _category) => [
    {
      name: "Nexus Components Ltd",
      rank: BigInt(1),
      tier: SupplierTier.Tier1,
      compositeRisk: BigInt(22),
      avgLeadTimeDays: BigInt(12),
      category: SupplierCategory.Electronics,
      supplierId: BigInt(10),
    },
    {
      name: "Precision Parts GmbH",
      rank: BigInt(2),
      tier: SupplierTier.Tier2,
      compositeRisk: BigInt(35),
      avgLeadTimeDays: BigInt(18),
      category: SupplierCategory.Electronics,
      supplierId: BigInt(11),
    },
  ],

  getRiskAlerts: async (_threshold) => [
    {
      qualityRisk: BigInt(85),
      delayRisk: BigInt(72),
      supplierName: "Apex Electronics Co.",
      compositeRisk: BigInt(89),
      triggeredAt: BigInt(Date.now() - 1800000) * BigInt(1000000),
      failureRisk: BigInt(78),
      riskLevel: RiskLevel.High,
      supplierId: BigInt(3),
    },
    {
      qualityRisk: BigInt(62),
      delayRisk: BigInt(58),
      supplierName: "Global Logistics Partners",
      compositeRisk: BigInt(68),
      triggeredAt: BigInt(Date.now() - 3600000) * BigInt(1000000),
      failureRisk: BigInt(55),
      riskLevel: RiskLevel.Medium,
      supplierId: BigInt(7),
    },
    {
      qualityRisk: BigInt(71),
      delayRisk: BigInt(65),
      supplierName: "Meridian Raw Materials",
      compositeRisk: BigInt(74),
      triggeredAt: BigInt(Date.now() - 7200000) * BigInt(1000000),
      failureRisk: BigInt(61),
      riskLevel: RiskLevel.High,
      supplierId: BigInt(4),
    },
  ],

  getSupplierProfiles: async () => [
    {
      id: BigInt(1),
      name: "Apex Electronics Co.",
      tier: SupplierTier.Tier1,
      category: SupplierCategory.Electronics,
      location: "Shenzhen, China",
      qualityRisk: BigInt(85),
      delayRisk: BigInt(72),
      failureRisk: BigInt(78),
      compositeRisk: BigInt(89),
      defectRate: 0.042,
      onTimeDeliveryRate: 0.78,
      carrierPerformanceScore: 0.71,
      equipmentFailureRate: 0.035,
      avgLeadTimeDays: BigInt(21),
      volumeHandled: BigInt(15000),
      supplyInterruptionCount: BigInt(3),
      yearsActive: BigInt(8),
      dependsOn: [],
    },
    {
      id: BigInt(2),
      name: "Meridian Raw Materials",
      tier: SupplierTier.Tier1,
      category: SupplierCategory.RawMaterials,
      location: "São Paulo, Brazil",
      qualityRisk: BigInt(71),
      delayRisk: BigInt(65),
      failureRisk: BigInt(61),
      compositeRisk: BigInt(74),
      defectRate: 0.028,
      onTimeDeliveryRate: 0.82,
      carrierPerformanceScore: 0.79,
      equipmentFailureRate: 0.021,
      avgLeadTimeDays: BigInt(35),
      volumeHandled: BigInt(28000),
      supplyInterruptionCount: BigInt(2),
      yearsActive: BigInt(12),
      dependsOn: [],
    },
    {
      id: BigInt(3),
      name: "Global Logistics Partners",
      tier: SupplierTier.Tier2,
      category: SupplierCategory.Logistics,
      location: "Rotterdam, Netherlands",
      qualityRisk: BigInt(62),
      delayRisk: BigInt(58),
      failureRisk: BigInt(55),
      compositeRisk: BigInt(68),
      defectRate: 0.015,
      onTimeDeliveryRate: 0.85,
      carrierPerformanceScore: 0.83,
      equipmentFailureRate: 0.018,
      avgLeadTimeDays: BigInt(14),
      volumeHandled: BigInt(42000),
      supplyInterruptionCount: BigInt(1),
      yearsActive: BigInt(15),
      dependsOn: [BigInt(1)],
    },
    {
      id: BigInt(4),
      name: "Pacific Chemicals Ltd",
      tier: SupplierTier.Tier2,
      category: SupplierCategory.Chemicals,
      location: "Seoul, South Korea",
      qualityRisk: BigInt(38),
      delayRisk: BigInt(29),
      failureRisk: BigInt(31),
      compositeRisk: BigInt(35),
      defectRate: 0.008,
      onTimeDeliveryRate: 0.94,
      carrierPerformanceScore: 0.91,
      equipmentFailureRate: 0.009,
      avgLeadTimeDays: BigInt(10),
      volumeHandled: BigInt(9500),
      supplyInterruptionCount: BigInt(0),
      yearsActive: BigInt(6),
      dependsOn: [BigInt(2)],
    },
    {
      id: BigInt(5),
      name: "Nordic Packaging Solutions",
      tier: SupplierTier.Tier3,
      category: SupplierCategory.Packaging,
      location: "Stockholm, Sweden",
      qualityRisk: BigInt(18),
      delayRisk: BigInt(15),
      failureRisk: BigInt(12),
      compositeRisk: BigInt(15),
      defectRate: 0.003,
      onTimeDeliveryRate: 0.97,
      carrierPerformanceScore: 0.95,
      equipmentFailureRate: 0.004,
      avgLeadTimeDays: BigInt(7),
      volumeHandled: BigInt(6200),
      supplyInterruptionCount: BigInt(0),
      yearsActive: BigInt(20),
      dependsOn: [],
    },
    {
      id: BigInt(6),
      name: "Vertex Manufacturing Corp",
      tier: SupplierTier.Tier1,
      category: SupplierCategory.Manufacturing,
      location: "Detroit, USA",
      qualityRisk: BigInt(45),
      delayRisk: BigInt(41),
      failureRisk: BigInt(38),
      compositeRisk: BigInt(43),
      defectRate: 0.019,
      onTimeDeliveryRate: 0.89,
      carrierPerformanceScore: 0.86,
      equipmentFailureRate: 0.022,
      avgLeadTimeDays: BigInt(18),
      volumeHandled: BigInt(21000),
      supplyInterruptionCount: BigInt(1),
      yearsActive: BigInt(10),
      dependsOn: [BigInt(1), BigInt(2)],
    },
  ],

  getSupplierTrendHistory: async (_supplierId, _days) => {
    const entries: RiskTrendEntry[] = [];
    const now = Date.now();
    for (let i = 29; i >= 0; i--) {
      const base = 50 + Math.sin(i / 5) * 15;
      entries.push({
        qualityRisk: BigInt(Math.round(base + Math.random() * 10 - 5)),
        delayRisk: BigInt(Math.round(base - 5 + Math.random() * 10 - 5)),
        compositeRisk: BigInt(Math.round(base + 8 + Math.random() * 10 - 5)),
        failureRisk: BigInt(Math.round(base - 10 + Math.random() * 10 - 5)),
        timestamp: BigInt(now - i * 86400000) * BigInt(1000000),
      });
    }
    return entries;
  },

  getSupplyNetworkGraph: async () => ({
    edgeCount: BigInt(5),
    nodes: [
      {
        dependsOn: [],
        supplierName: "Apex Electronics Co.",
        tier: SupplierTier.Tier1,
        compositeRisk: BigInt(89),
        category: SupplierCategory.Electronics,
        supplierId: BigInt(1),
      },
      {
        dependsOn: [],
        supplierName: "Meridian Raw Materials",
        tier: SupplierTier.Tier1,
        compositeRisk: BigInt(74),
        category: SupplierCategory.RawMaterials,
        supplierId: BigInt(2),
      },
      {
        dependsOn: [BigInt(1)],
        supplierName: "Global Logistics Partners",
        tier: SupplierTier.Tier2,
        compositeRisk: BigInt(68),
        category: SupplierCategory.Logistics,
        supplierId: BigInt(3),
      },
      {
        dependsOn: [BigInt(2)],
        supplierName: "Pacific Chemicals Ltd",
        tier: SupplierTier.Tier2,
        compositeRisk: BigInt(35),
        category: SupplierCategory.Chemicals,
        supplierId: BigInt(4),
      },
      {
        dependsOn: [],
        supplierName: "Nordic Packaging Solutions",
        tier: SupplierTier.Tier3,
        compositeRisk: BigInt(15),
        category: SupplierCategory.Packaging,
        supplierId: BigInt(5),
      },
    ],
  }),

  logMitigationAction: async (_supplierId, _action, _dueDate) => BigInt(3),

  simulateDisruption: async (supplierId, disruptionType) => ({
    estimatedRecoveryDays: BigInt(14),
    cascadingSupplierIds: [BigInt(3), BigInt(6)],
    affectedSupplierId: supplierId,
    directRiskIncrease: BigInt(25),
    disruptionType: disruptionType,
    portfolioRiskDelta: BigInt(12),
    severity: RiskLevel.High,
  }),

  updateSupplierMetrics: async (_supplierId, _metrics) => true,
};
